import streamlit as st


def render_driver_view():
    st.header(
        "Driver Portal"
    )

    st.caption(
        "Accept rescue assignments, "
        "confirm pickup and complete deliveries."
    )

    st.info(
        "Driver assignments will be connected "
        "to RescueMesh operations next."
    )